import { Layout, StyleProvider, NavLinks } from 'mdx-go'
import * as components from '../public/components'

export const Root = props =>
  <Layout>
    <Layout.MenuToggle />
    <Layout.Sidebar>
      <NavLinks {...props} />
    </Layout.Sidebar>
    <Layout.Main>
        <StyleProvider components={components}>
            {props.children}
        </StyleProvider>
    </Layout.Main>
  </Layout>

# Hardy

Geckoboard's Design System
