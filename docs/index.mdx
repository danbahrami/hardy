import { Layout, StyleProvider, NavLinks } from 'mdx-go'

export const Root = props =>
  <Layout>
    <Layout.MenuToggle />
    <Layout.Sidebar>
      <NavLinks {...props} />
    </Layout.Sidebar>
    <Layout.Main>
        <StyleProvider>
            {props.children}
        </StyleProvider>
    </Layout.Main>
  </Layout>

# Hardy

Geckoboard's Design System
